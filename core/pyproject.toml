[build-system]
requires = ["setuptools>=64", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "omp_core"
version = "0.1.0"
description = "Orbital Mission Planner C++ core (two-body RK4 + SGP4) with pybind11 bindings"
requires-python = ">=3.9"
